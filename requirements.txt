# ===== 核心依赖 =====
# Web 框架
streamlit>=1.28.0
fastapi>=0.104.0
uvicorn>=0.24.0

# LLM 调用
openai>=1.0.0
python-dotenv>=1.0.0

# 语义检索
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4

# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# 可视化
plotly>=5.17.0
matplotlib>=3.7.0

# 工具
pydantic>=2.0.0
requests>=2.31.0

# ===== 说明 =====
# sentence-transformers: 本地 Embedding 模型，支持中文
# faiss-cpu: 向量检索，CPU 版本（如有 GPU 可换 faiss-gpu）
# streamlit: 前端界面
# fastapi: 后端微服务
# openai: 兼容多家大模型 API（OpenAI / 通义 / DeepSeek / MiniMax）
